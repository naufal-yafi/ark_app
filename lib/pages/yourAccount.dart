import 'package:flutter/material.dart';

class YourAccountPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'arK | Artikel Kulo',
      home: Scaffold(
        body: Text("Your Account"),
      ),
      debugShowCheckedModeBanner: false,
    );
  }
}
